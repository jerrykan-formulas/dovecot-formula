dovecot:
  service: dovecot
  packages:
    - dovecot-imapd

  config_conf_dir: /etc/dovecot
  config_confd_dir: /etc/dovecot/conf.d

  virtual_user_db_dir: /var/lib/dovecot/authdb
  virtual_user_db_file: authdb.sqlite3
  virtual_user_uid: vmail
  virtual_user_home: /home/vmail
